import verify from "./verify"
import add from "./add"
import sendCode from './sendCode';
import setDeletionDate from './setDeletionDate';

const emailController = { verify, add, sendCode, setDeletionDate };
export default emailController
